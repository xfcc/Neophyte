# Neophyte Public View

由 Neophyte 本地项目生成的只读静态页面。公开数据采用字段白名单，只包含产品名称、日期、状态、比例、涨跌幅和收益率；不包含份额、数量、成本、金额、净值、价格、汇率、配置页或写入接口。

请把本目录作为独立 Git 仓库根目录关联 Vercel，不要把上级本地项目整体推送。页面设置了 noindex 和 robots.txt，但这不能阻止知道网址的人访问；需要私密访问时，应在托管平台启用部署保护。
